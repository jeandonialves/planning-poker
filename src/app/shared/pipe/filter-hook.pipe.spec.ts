import { FilterHookPipe } from './filter-hook.pipe';

describe('FilterHookPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterHookPipe();
    expect(pipe).toBeTruthy();
  });
});
